date: 2026-03-15
project: RAMSpeed
status: in_progress
current_focus:
  - residual elevated desktop smoke verification in a clean machine state
  - documenting cleanup-wave evidence and remaining risks
  - residual desktop smoke verification
completed:
  - inspected Rust/WPF repo layout and confirmed active tray logic is in the WPF app
  - reproduced root-cause hypotheses from code review and parallel subagent audits
  - verified the WPF app builds locally with dotnet build
  - added xUnit regression tests for tray tooltip formatting and single-instance activation signaling
  - replaced title-only instance activation with a primary-instance activation signal plus fallback activation path
  - routed tray tooltip updates from raw memory snapshots and marshaled memory updates back to the WPF dispatcher
  - added regression tests for installer payload copying and scheduled-task XML generation
  - replaced self-install single-file copy with full payload copy
  - removed the Run-key startup path and unified autostart around the scheduled task definition
  - updated the Inno Setup script to install the full publish payload instead of just the exe
  - updated the task definition so the scheduled task persists for silent elevation even when logon startup is disabled
  - added app startup registration flags and a non-admin handoff path that tries the scheduled task before prompting for UAC
  - added unit coverage for self-trim policy rules and `MemoryInfoService` disposal semantics
  - implemented `SelfTrimPolicy` and reason-based self-trim execution for startup, periodic, and post-optimization paths
  - removed the old per-tick aggressive self-trim and the optimizer-level unconditional trim
  - made `MemoryInfoService`, `MemoryOptimizer`, and `MemoryMonitor` disposable and routed shutdown through monitor disposal
  - moved startup self-trim ownership into `MemoryMonitor.Start()` and removed the direct startup trim from the window code
  - published a fresh `win-x64` portable payload and verified it contains `RAMSpeed.exe`, `RAMSpeed.dll`, `RAMSpeed.deps.json`, and `RAMSpeed.runtimeconfig.json`
  - observed a live close-to-tray signal: invoking the running window's close path left the `RAMSpeed.exe` process resident instead of exiting
  - observed a live single-instance signal: relaunching `publish\\RAMSpeed.exe` while one instance was already running left the process count unchanged at 1
  - reran `dotnet test` and `dotnet build` successfully after the cleanup-wave changes
  - reran Trivy via the shell fallback after the MCP wrapper failed; no HIGH/CRITICAL vulnerability, misconfiguration, secret, or license findings were reported
  - captured smoke preconditions showing a non-admin shell, no `RAMSpeed` scheduled task, and pre-existing Program Files and Start Menu install artifacts
pending:
  - manual elevated desktop smoke test of hide-to-tray and restore behavior in a clean session
  - manual portable self-install smoke test from `publish\\RAMSpeed.exe` after removing stale installed artifacts
  - manual install/startup smoke test from the Inno Setup path after portable smoke is green
open_risks:
  - restore behavior after tray-hide is not covered by an automated UI integration test
  - tray tooltip refresh while hidden is still not directly validated in a live desktop session
  - startup task creation/update is unit-verified through XML generation and code path review, not a live Task Scheduler smoke test
  - the current workstation still contains a stale `C:\\Program Files\\RAMSpeed` footprint from an earlier broken install, so local install-state observations are not yet authoritative for the new payload logic
  - an elevated `RAMSpeed.exe` instance remained running during the smoke attempt and could not be terminated from this shell, which prevented a clean end-to-end desktop validation pass
